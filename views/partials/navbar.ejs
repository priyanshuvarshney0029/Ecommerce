<nav class="navbar fixed-top navbar-expand-lg navbar-dark" style="background-color: #333;">
  <div class="container">
    <!-- Logo and Brand -->
    <a class="navbar-brand" href="/products" style="color: #f8f9fa;">
      <i class="fas fa-shopping-basket" style="margin-right: 10px; font-size: 1.7rem;"></i>Shopping Cart
    </a>
    
    <!-- Toggler button for mobile view -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarTogglerDemo02" aria-controls="navbarTogglerDemo02" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Navbar links and User section -->
    <div class="collapse navbar-collapse" id="navbarTogglerDemo02">
      <!-- Left-aligned links -->
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link text-white" aria-current="page" href="/">Home</a>
        </li>
        <li class="nav-item">
          <a class="nav-link text-white" href="/products">Products</a>
        </li>
        <% if (currentUser && currentUser.role === 'seller') { %>
        <li class="nav-item">
          <a class="nav-link text-white" href="/products/new">New</a>
        </li>
        <% } %>
      </ul>

      <!-- Right-aligned User Account and Cart Section -->
      <div class="navbar-nav ms-auto">
        <% if (!currentUser) { %>
          <a href="/login" class="nav-link text-white">Login</a>
        <% } else { %>
          <span class="nav-link text-white text-capitalize">Hello, <%= currentUser.username %></span>
          <a href="/user/cart" class="nav-link text-white position-relative">
            <i class="fas fa-shopping-cart"></i>
            <sup><span class="badge bg-danger position-absolute top-0 start-100 translate-middle badge-cart-count"><%= currentUser.cart.length %></span></sup>
          </a>
          <a href="/logout" class="nav-link text-white">Logout</a>
        <% } %>
      </div>
    </div>
  </div>
</nav>

<!-- Custom Styling -->
<style>
  /* Navbar background and text colors */
  .navbar {
    background-color: #333 !important; /* Lightish black background */
  }
  .navbar-brand, .nav-link {
    color: #f8f9fa !important; /* White text */
  }

  /* Navbar hover effect */
  .navbar .nav-link:hover {
    color: #ddd !important; /* Slightly lighter on hover */
  }

  /* Badge styling for cart count */
  .badge-cart-count {
    font-size: 0.7rem;
    padding: 0.3em 0.6em;
    border-radius: 50%;
  }

  /* Toggler button icon color */
  .navbar-toggler-icon {
    filter: invert(1); /* Makes toggler icon white */
  }
</style>
